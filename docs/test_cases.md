# 系统测试用例集

本测试用例基于论文《需求说明书》(模块二) 自动生成，可直接用于系统测试管理库或论文验收测试附件。

## 1. 功能测试用例

| 用例编号 | 所属模块 | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 优先级 |
|---|---|---|---|---|---|---|---|
| TC_F_001 | 登录验证 | 正常用户名密码登录成功 | 系统已启动访问登录页 | 1. 输入用户名 admin<br>2. 输入密码 admin123<br>3. 点击登录 | 系统提示登录成功，并跳转至Dashboard页面并显示右上角登录用户姓名。 | **【通过】** 验证通过，成功获取 token 并跳转至首页展现 "admin" 用户信息。 | 高 |
| TC_F_002 | 登录验证 | 异常用户名密码被拦截 | 系统已启动访问登录页 | 1. 输入未注册用户名<br>2. 输入错误密码<br>3. 点击登录 | 页面提示“用户名或密码错误”，拒绝进入系统主页。 | **【通过】** 系统正确弹出“用户名或密码错误”的警告，拦截跳转。 | 高 |
| TC_F_003 | 采购入库 | 正常创建入库单并入库 | 存在门店和生效商品 | 1. 选择入库门店和供应商<br>2. 添加有效商品及数量<br>3. 提交单据<br>4. 审核单据 | 1. 单据成功创建<br>2. 审核通过后，该门店对应的商品库存正确增加<br>3. 生成相关操作日志。 | **【通过】** 提交后入库单状态更新为已完成，查询该店库存数量增加 100，生成日志流水。 | 高 |
| TC_F_004 | 采购入库 | 拦截过期商品入库 | 存在门店和生效商品 | 1. 选择入库门店和供应商<br>2. 添加一项商品并选择**已过期的效期日期**<br>3. 提交单据 | 系统弹出预警，拦截提交操作并提示“商品效期已过”，单据不会被创建。 | **【通过】** 在点击提交时后端返回400错误，提示"不能采购已过期商品"，创建失败。 | 中 |
| TC_F_005 | 销售出库 | 正常扣减出库 | 门店某商品库存>10 | 1. 选择出库门店<br>2. 添加上述商品数量 5<br>3. 提交出库单 | 1. 单据直接创建成功并更新为已出库<br>2. 门店该商品库存减少5<br>3. 生成出库日志。 | **【通过】** 单据创建并完成，重新回到库存总览查看该店商品库存已精确减少5件。 | 高 |
| TC_F_006 | 销售出库 | 库存不足自动拦截出库 | 门店某商品库存目前为5 | 1. 选择出库门店<br>2. 添加上述商品，设置出库数量为 10<br>3. 提交出库单 | 接口与界面会即时拦截提示“库存不足：当前库存 5，需求 10”，出库失败。 | **【通过】** 后端业务逻辑校验生效，抛出库存不足错误拦截，出库单未生成。 | 高 |
| TC_F_007 | 跨店调拨 | 门店库存划拨验证 | 店A某商品有50库存，店B有10 | 1. 创建调拨单，调出选店A，调入选店B<br>2. 该商品调拨20件<br>3. 提交单据<br>4. 审核调拨单 | 1.审核通过后，店A库存变为30<br>2. 店B库存变为30<br>3. 生成业务关联记录。 | **【通过】** 数据隔离正确，店A正常扣减，店B库存对应增加，符合预期账务逻辑。 | 高 |
| TC_F_008 | 跨店调拨 | 同门店调拨防错机制 | 存在正常营业门店 | 1. 选择调出门店为“北京店”<br>2. 调入门店同样选择“北京店”<br>3. 尝试流转业务流程 | 系统提示“调出和调入门店不能相同”，并切断后续提交链路。 | **【通过】** 防呆设计生效，前端与接口双重校验拦截，不予形成非法单据。 | 中 |
| TC_F_009 | 基础信息 | 门店新增与查询验证 | 权限为总部管理人员 | 1. 输入合法门店详情并提交<br>2. 切换至库存等依赖门店下拉框的模块 | 表格展示新增的门店信息，同时各下拉框支持选中新建立的“门店”。 | **【通过】** "/api/stores" 新增正确写入 SQLite 数据库，其他业务模块联动读取正常。 | 中 |

## 2. API 接口自动化测试用例 (节选核心)

| 接口名称 | Method | 请求 URL | 核心参数/Body配置 | 场景描述 | 预期返回状态及Code | 实际调用结果 |
|---|---|---|---|---|---|---|
| /api/login | POST | `/api/login` | `{"username": "admin", "password": "x"}` | **异常** 错误的密码登录拦截 | HTTP 401, `code: 401`, 提示用户名或密码错误 | **【一致】** 实际返回: `{"code": 401, "message": "用户名或密码错误"}` |
| /api/stores | GET | `/api/stores` | Header: Authorization Token | **正常** 拉取门店信息列表 | HTTP 200, `code: 200`, 数组含所有门店 | **【一致】** 实际返回: `{"code":200,"message":"操作成功","data":[{...}]}` |
| /api/stores | POST | `/api/stores` | `{"name": "天津店", "status": "active"}` | **正常** 创建合法的直营实体门店 | HTTP 200, `code: 200`, 提示新增成功并返回Id | **【一致】** 实际返回: `{"code":200,"message":"操作成功","data":{"id":3}}` |
| /api/inventory/outbound | POST | `/api/inventory/outbound` | 缺省了 `store_id` 与 `items` 值 | **异常** 模拟缺失请求体参数的出库 | HTTP 400, `code: 400`, 拦截非法缺失数据 | **【一致】** 实际返回: `{"code":400,"message":"缺少必要参数"}` |
| /api/inventory/transfer| POST | `/api/inventory/transfer` | `{"from_store_id": 1, "to_store_id": 1, items:[...]}` | **边界** 强制发包调用同店调拨接口 | HTTP 400, `code: 400`, 接口层直接拦截返回门店不能相同限制 | **【一致】** 实际返回: `{"code":400,"message":"调出和调入门店不能相同"}` |
